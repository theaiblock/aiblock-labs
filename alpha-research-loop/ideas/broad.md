Improve the benchmark signal.

It is a simple time-series momentum strategy: it holds every coin whose 28-day return is positive,
equally weighted, and rebalances weekly. Propose one change that you expect to raise its
risk-adjusted return, using only what the contract provides. It can be any kind of change to which
coins are held or how much of each.
